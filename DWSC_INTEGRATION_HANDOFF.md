# DarkWave Smart Chain Integration Request

## From: Pulse (darkwavepulse.com)
## To: DWTL/DWSC Agent

---

## Purpose
Pulse needs to connect to the DarkWave Smart Chain to enable:
- Prediction hash verification (stamping AI predictions on-chain)
- Hallmark generation for user accounts
- Cross-platform verification badges

---

## Required Information

Please provide the following credentials for Pulse integration:

| Variable | Description |
|----------|-------------|
| `DARKWAVE_CHAIN_URL` | The base URL of the DWSC API (e.g., `https://chain.darkwave.io` or `https://your-dwsc-domain.com`) |
| `DARKWAVE_API_KEY` | API key for Pulse to authenticate with DWSC |
| `DARKWAVE_API_SECRET` | API secret for HMAC signature authentication |

---

## API Endpoints Pulse Will Use

Once connected, Pulse will call these DWSC endpoints:

1. **Authentication**: `POST /api/developer/auth`
2. **Hash Submit**: `POST /api/hash/submit` (for prediction verification)
3. **Hash Verify**: `GET /api/hash/verify/{hash}`
4. **Hallmark Generate**: `POST /api/hallmark/generate`
5. **Hallmark Verify**: `GET /api/hallmark/{id}/verify`
6. **Chain Status**: `GET /api/status`

---

## Request Headers Pulse Will Send

```
Content-Type: application/json
X-Api-Key: {DARKWAVE_API_KEY}
X-Timestamp: {unix_timestamp}
X-Signature: {HMAC-SHA256 signature}
X-Developer-Session: {session_token after auth}
```

---

## Signature Generation

Pulse generates signatures using:
```
message = "{METHOD}:{PATH}:{BODY}:{TIMESTAMP}"
signature = HMAC-SHA256(message, DARKWAVE_API_SECRET)
```

---

## App Registration Details

| Field | Value |
|-------|-------|
| App Name | Pulse |
| Source App ID | `pulse` |
| Data Types | `prediction`, `hallmark` |

---

## Next Steps

1. Create/retrieve API credentials for Pulse on the DWSC developer portal
2. Provide the three values above (URL, API_KEY, API_SECRET)
3. Pulse will configure and test the connection

---

*Generated by Pulse - DarkWave Studios, LLC*
